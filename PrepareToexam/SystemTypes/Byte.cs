namespace SystemTypes
{
    class Byte
    {
        // Inheritance Object -> ValueType -> Byte.
        // Aliase in c# is byte.
        // Size is 1 byte.
        // Range from 0 to 255.
        // Default valu is 0.
        readonly byte Default = 0;
        readonly byte min = byte.MinValue;// 0;
        readonly byte max = byte.MaxValue;// 255;

    }
}
